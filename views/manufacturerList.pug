extends layout

block content
  h1= title
  p We are collaborating with the next companies
  if manufacturerList.length
    ul
      each manufacturer in manufacturerList
        li
          a(href=manufacturer.url) #{manufacturer.name}
  else
    p There are no manufacturer yet.
