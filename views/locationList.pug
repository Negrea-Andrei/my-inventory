extends layout

block content
  h1= title
  p You can find our store at the following locations
  if locationList.length
    ul
      each location in locationList
        li
          a(href=location.url) #{location.name} 
          | located at #{location.address}
  else
    p There are no locations available.